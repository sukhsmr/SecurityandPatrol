import React from 'react';
import SingleBlogClient from "@/components/sections/SingleBlogClient";

export const metadata = {
  title: 'Security Officer vs. Security Guard | ADS Guards Blog',
  description: 'Security officer vs. security guard — learn the real differences in titles, duties, California law, and armed status before hiring security staff.',
};

export default function SingleBlogPage() {
  return <SingleBlogClient />;
}
